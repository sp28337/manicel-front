name: CI/CD Frontend

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read
  pull-requests: read

jobs:
  build:
    name: Lint & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js (LTS)
        uses: actions/setup-node@v4
        with:
          node-version: "24.11.1"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

      - name: Build Next.js
        run: npm run build

  deploy:
    name: Deploy to Production
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: manicel

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          port: ${{ secrets.PORT }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -euo pipefail

            echo "🚀 Starting frontend deployment..."

            cd ~/manicel/frontend || { echo "❌ Failed to change directory"; exit 1; }

            # Load NVM
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"

            # Use LTS Node
            nvm use 24
            node -v
            npm -v

            echo "📦 Pulling latest code..."
            git fetch origin main
            git checkout main
            git pull origin main

            echo "📥 Installing dependencies..."
            npm ci

            echo "🏗️ Building frontend..."
            npm run build

            echo "🔄 Restarting service..."
            sudo systemctl daemon-reload
            sudo systemctl restart manicel-frontend.service

            echo "⏳ Waiting for service..."
            sleep 3

            if ! sudo systemctl is-active --quiet manicel-frontend.service; then
              echo "❌ Frontend service failed!"
              sudo journalctl -u manicel-frontend.service -n 50 --no-pager
              exit 1
            fi

            echo "🏥 Health check..."
            if ! curl -sf http://localhost:3000 > /dev/null 2>&1; then
              echo "⚠️ Frontend not responding yet (may still be warming up)"
            fi

            echo "✅ Frontend deployment successful!"
